# Spam-Email-Classifier
A Python ML project that uses NLP to clean emails, extracts features with TF-IDF/Bag of Words, and classifies them as Spam or Not Spam using Naive Bayes/SVM, achieving 97–99% accuracy. Can be integrated into email systems to filter unwanted messages automatically.
